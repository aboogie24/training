config:
  aws-native:region: us-east-1
  training:Environment: dev
  training:name: Training
  training:subnets:
    values:
      private:
        - 10.0.1.0/24
      public:
        - 10.0.2.0/24
  training:vpc:
    values:
      cidrBlock: 10.0.0.0/16
